<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title><%= content_for?(:title) ? yield(:title) + " | Trip-payzies(トリップペイジーズ)" : "Trip-payzies(トリップペイジーズ)" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png?v=2" type="image/png">
    <link rel="icon" href="/icon.svg?v=2" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%# iconfy[アイコンパック]を導入 %> 
    <script src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js"></script>
    <%= javascript_importmap_tags %>
  </head>

  <% unless controller.controller_name == "homes" %>
    <body class="bg-white">
      <div class="page-width">

        <header class="h-15 bg-panel-navy flex items-center justify-between border-2 border-black">
            <% unless controller.controller_name == "homes"%>
              <%= render "layouts/header"%>
            <% end %>
        </header>

        <p class="notice bg-primary-blue-100 text-white text-sm "><%= notice %></p>
        <p class="alert bg-primary-red text-white text-sm "><%= alert %></p>

        <main class="bg-form-gray flex-1 min-h-0 flex flex-col border-2  border-x-black border-y-form-gray">
            <%= yield %>
        </main>

        <footer class="h-10 flex items-center justify-center bg-panel-navy border-2 border-black">
            <% unless controller.controller_name == "homes"%>
              <%= render "layouts/footer"%>
            <% end %>
        </footer>

        <% unless devise_controller? %>
          <%= render "layouts/sidebar"%>
        <% end %>

      </div>
    </body>

  <% else %>
    <%= yield %> 
  <% end %>



</html>
