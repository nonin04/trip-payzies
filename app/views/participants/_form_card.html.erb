<div class="font-bold">参加者</div>

<div class="card-style flex flex-col px-4 pt-4 pb-8">

  <%= f.fields_for :participants do |p| %>
    <div class="relative">
      <%= p.text_field :name, class:"form-style #{'!border-red-500 !bg-red-100' if p.object.errors[:name].any?}" %>
      <div class="absolute top-1/2 -translate-y-1/2 right-4 text-white bg-primary-red rounded px-2">削除</div>
    </div>
  <% end %>
  

</div>